PROJECT(algo)

CMAKE_MINIMUM_REQUIRED(3.21)

CMAKE_C_STANDARD(23)
CMAKE_CXX_STANDARD(23)

FIND_PACKAGE(Boost)

if (Boost_FOUND)
    INCLUDE_DIRECTORIES(${Boost_INCLUDE_DIRS})
endif()

ADD_EXECUTABLE(algo main.c util.c)